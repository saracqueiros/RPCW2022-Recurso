const mongoose = require('mongoose')

var Fracao = require('../models/fracao')

/*
module.exports.listarNomes = () => {
    return Cidade
        .find({}, { '_id': 0, 'nome': 1 })
        .sort({ nome: 1 })
        .exec()
}
*/